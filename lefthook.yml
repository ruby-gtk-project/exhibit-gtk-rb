# Git hooks managed by lefthook. Enable in a fresh clone with: lefthook install
# (the flake devShell provides `lefthook` and `git-lfs`).
pre-commit:
  parallel: true
  commands:
    lfs-guard:
      # Refuse to commit binaries that aren't in Git LFS.
      run: scripts/lfs-guard.sh
